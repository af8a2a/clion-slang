diff --git a/source/slang/slang-language-server-semantic-tokens.cpp b/source/slang/slang-language-server-semantic-tokens.cpp
index 598452d5e..09d3829dd 100644
--- a/source/slang/slang-language-server-semantic-tokens.cpp
+++ b/source/slang/slang-language-server-semantic-tokens.cpp
@@ -28,7 +28,9 @@ const char* kSemanticTokenTypes[] = {
     "enum",
     "typeParameter",
     "method",
-    "decorator"};
+    "decorator",
+    "slangSemantic",
+    "slangSwizzle"};
 
 const char* kSemanticTokenModifiers[] =
     {"declaration", "definition", "readonly", "static", "defaultLibrary"};
@@ -188,6 +190,14 @@ bool isHighlightableName(Name* name)
     return true;
 }
 
+static bool _isScalarOrVectorSwizzle(SwizzleExpr* expr)
+{
+    auto baseType = expr && expr->base ? expr->base->type.type : nullptr;
+    while (auto modifiedType = as<ModifiedType>(baseType))
+        baseType = modifiedType->getBase();
+    return as<BasicExpressionType>(baseType) || as<VectorExpressionType>(baseType);
+}
+
 List<SemanticToken> getSemanticTokens(
     Linkage* linkage,
     Module* module,
@@ -256,10 +266,38 @@ List<SemanticToken> getSemanticTokens(
                         return;
                     if (as<ImplicitParameterGroupElementTypeModifier>(modifier))
                         return;
+                    if (auto semantic = as<HLSLSemantic>(modifier))
+                    {
+                        SemanticToken token =
+                            _createSemanticToken(manager, semantic->name.loc, nullptr);
+                        token.length = (int)semantic->name.getContentLength();
+                        token.type = SemanticTokenType::SlangSemantic;
+                        maybeInsertToken(token);
+                    }
                 }
             }
 
-            if (auto declRefExpr = as<DeclRefExpr>(node))
+            if (auto swizzleExpr = as<SwizzleExpr>(node))
+            {
+                // Tuple element selection also lowers to SwizzleExpr. Only
+                // scalar/vector bases represent Slang/HLSL swizzle syntax.
+                if (_isScalarOrVectorSwizzle(swizzleExpr))
+                {
+                    SemanticToken token = _createSemanticToken(manager, swizzleExpr->loc, nullptr);
+                    token.length = doc->getTokenLength(token.line, token.col);
+                    token.type = SemanticTokenType::SlangSwizzle;
+                    maybeInsertToken(token);
+                }
+            }
+            else if (auto matrixSwizzleExpr = as<MatrixSwizzleExpr>(node))
+            {
+                SemanticToken token =
+                    _createSemanticToken(manager, matrixSwizzleExpr->loc, nullptr);
+                token.length = doc->getTokenLength(token.line, token.col);
+                token.type = SemanticTokenType::SlangSwizzle;
+                maybeInsertToken(token);
+            }
+            else if (auto declRefExpr = as<DeclRefExpr>(node))
             {
                 handleDeclRef(
                     declRefExpr->declRef,
@@ -516,9 +554,28 @@ static SemanticTokenType _getStockFallbackType(SemanticTokenType type)
     }
 }
 
-static uint32_t _getEncodedType(const SemanticToken& token, bool useEnhancedSemanticTokens)
+static SemanticTokenType _getM2aFallbackType(SemanticTokenType type)
 {
-    return (uint32_t)(useEnhancedSemanticTokens ? token.type : _getStockFallbackType(token.type));
+    switch (type)
+    {
+    case SemanticTokenType::SlangSemantic:
+        return SemanticTokenType::EnumMember;
+    case SemanticTokenType::SlangSwizzle:
+        return SemanticTokenType::Property;
+    default:
+        return type;
+    }
+}
+
+static uint32_t _getEncodedType(
+    const SemanticToken& token,
+    bool useEnhancedSemanticTokens,
+    bool useSlangSemanticTokens)
+{
+    auto type = useSlangSemanticTokens ? token.type : _getM2aFallbackType(token.type);
+    if (!useEnhancedSemanticTokens)
+        type = _getStockFallbackType(type);
+    return (uint32_t)type;
 }
 
 static uint32_t _getEncodedModifiers(const SemanticToken& token, bool useEnhancedSemanticTokens)
@@ -526,7 +583,10 @@ static uint32_t _getEncodedModifiers(const SemanticToken& token, bool useEnhance
     return useEnhancedSemanticTokens ? token.modifiers : 0;
 }
 
-List<uint32_t> getEncodedTokens(List<SemanticToken>& tokens, bool useEnhancedSemanticTokens)
+List<uint32_t> getEncodedTokens(
+    List<SemanticToken>& tokens,
+    bool useEnhancedSemanticTokens,
+    bool useSlangSemanticTokens)
 {
     List<uint32_t> result;
     if (tokens.getCount() == 0)
@@ -538,7 +598,8 @@ List<uint32_t> getEncodedTokens(List<SemanticToken>& tokens, bool useEnhancedSem
     result.add((uint32_t)tokens[0].line);
     result.add((uint32_t)tokens[0].col);
     result.add((uint32_t)tokens[0].length);
-    result.add(_getEncodedType(tokens[0], useEnhancedSemanticTokens));
+    result.add(
+        _getEncodedType(tokens[0], useEnhancedSemanticTokens, useSlangSemanticTokens));
     result.add(_getEncodedModifiers(tokens[0], useEnhancedSemanticTokens));
 
     // Encode the rest tokens as deltas.
@@ -557,7 +618,8 @@ List<uint32_t> getEncodedTokens(List<SemanticToken>& tokens, bool useEnhancedSem
         result.add(deltaLine);
         result.add(deltaCol);
         result.add((uint32_t)tokens[i].length);
-        result.add(_getEncodedType(tokens[i], useEnhancedSemanticTokens));
+        result.add(
+            _getEncodedType(tokens[i], useEnhancedSemanticTokens, useSlangSemanticTokens));
         result.add(_getEncodedModifiers(tokens[i], useEnhancedSemanticTokens));
 
         prevLine = thisLine;
diff --git a/source/slang/slang-language-server-semantic-tokens.h b/source/slang/slang-language-server-semantic-tokens.h
index 2688abc28..6f96e6a24 100644
--- a/source/slang/slang-language-server-semantic-tokens.h
+++ b/source/slang/slang-language-server-semantic-tokens.h
@@ -30,6 +30,11 @@ enum class SemanticTokenType
     TypeParameter,
     Method,
     Decorator,
+    // Slang/HLSL-specific entries are append-only and require explicit client
+    // negotiation. Clients that only support M2a receive standard LSP
+    // fallbacks for these classifications.
+    SlangSemantic,
+    SlangSwizzle,
     NormalText
 };
 extern const char* kSemanticTokenTypes[(int)SemanticTokenType::NormalText];
@@ -66,6 +71,9 @@ List<SemanticToken> getSemanticTokens(
     Module* module,
     UnownedStringSlice fileName,
     DocumentVersion* doc);
-List<uint32_t> getEncodedTokens(List<SemanticToken>& tokens, bool useEnhancedSemanticTokens);
+List<uint32_t> getEncodedTokens(
+    List<SemanticToken>& tokens,
+    bool useEnhancedSemanticTokens,
+    bool useSlangSemanticTokens);
 
 } // namespace Slang
diff --git a/source/slang/slang-language-server.cpp b/source/slang/slang-language-server.cpp
index 4e813fb55..69ee9914f 100644
--- a/source/slang/slang-language-server.cpp
+++ b/source/slang/slang-language-server.cpp
@@ -65,14 +65,29 @@ static bool _containsSemanticTokenName(const List<String>& values, const char* r
     return false;
 }
 
-static bool _supportsEnhancedSemanticTokens(const InitializeParams& args)
+static bool _supportsSemanticTokenRange(
+    const List<String>& values,
+    const char* const* required,
+    Index begin,
+    Index end)
 {
-    const auto& semanticTokens = args.capabilities.textDocument.semanticTokens;
-    for (Index i = 0; i < SLANG_COUNT_OF(kSemanticTokenTypes); ++i)
+    for (Index i = begin; i < end; ++i)
     {
-        if (!_containsSemanticTokenName(semanticTokens.tokenTypes, kSemanticTokenTypes[i]))
+        if (!_containsSemanticTokenName(values, required[i]))
             return false;
     }
+    return true;
+}
+
+static bool _supportsEnhancedSemanticTokens(const InitializeParams& args)
+{
+    const auto& semanticTokens = args.capabilities.textDocument.semanticTokens;
+    if (!_supportsSemanticTokenRange(
+            semanticTokens.tokenTypes,
+            kSemanticTokenTypes,
+            0,
+            (Index)SemanticTokenType::SlangSemantic))
+        return false;
     for (Index i = 0; i < SLANG_COUNT_OF(kSemanticTokenModifiers); ++i)
     {
         if (!_containsSemanticTokenName(semanticTokens.tokenModifiers, kSemanticTokenModifiers[i]))
@@ -81,9 +96,21 @@ static bool _supportsEnhancedSemanticTokens(const InitializeParams& args)
     return true;
 }
 
+static bool _supportsSlangSemanticTokens(const InitializeParams& args)
+{
+    const auto& tokenTypes = args.capabilities.textDocument.semanticTokens.tokenTypes;
+    return _supportsSemanticTokenRange(
+        tokenTypes,
+        kSemanticTokenTypes,
+        (Index)SemanticTokenType::SlangSemantic,
+        (Index)SemanticTokenType::NormalText);
+}
+
 SlangResult LanguageServerCore::init(const InitializeParams& args)
 {
     m_useEnhancedSemanticTokens = _supportsEnhancedSemanticTokens(args);
+    m_useSlangSemanticTokens =
+        m_useEnhancedSemanticTokens && _supportsSlangSemanticTokens(args);
     m_workspaceFolders = args.workspaceFolders;
     m_workspace = new Workspace();
     List<URI> rootUris;
@@ -190,8 +217,10 @@ SlangResult LanguageServer::parseNextMessage()
                     caps.signatureHelpProvider.triggerCharacters.add("<");
                     caps.signatureHelpProvider.triggerCharacters.add(",");
                     caps.signatureHelpProvider.retriggerCharacters.add(",");
-                    Index tokenTypeCount = m_core.m_useEnhancedSemanticTokens
+                    Index tokenTypeCount = m_core.m_useSlangSemanticTokens
                                                ? SLANG_COUNT_OF(kSemanticTokenTypes)
+                                           : m_core.m_useEnhancedSemanticTokens
+                                               ? (Index)SemanticTokenType::SlangSemantic
                                                : (Index)SemanticTokenType::Class;
                     for (Index i = 0; i < tokenTypeCount; ++i)
                         caps.semanticTokensProvider.legend.tokenTypes.add(kSemanticTokenTypes[i]);
@@ -1521,7 +1550,10 @@ LanguageServerResult<LanguageServerProtocol::SemanticTokens> LanguageServerCore:
     }
     SemanticTokens response;
     response.resultId = "";
-    response.data = getEncodedTokens(tokens, m_useEnhancedSemanticTokens);
+    response.data = getEncodedTokens(
+        tokens,
+        m_useEnhancedSemanticTokens,
+        m_useSlangSemanticTokens);
     return response;
 }
 
diff --git a/source/slang/slang-language-server.h b/source/slang/slang-language-server.h
index 36d2c1009..d256d8f95 100644
--- a/source/slang/slang-language-server.h
+++ b/source/slang/slang-language-server.h
@@ -106,6 +106,7 @@ public:
     List<LanguageServerProtocol::WorkspaceFolder> m_workspaceFolders;
     LanguageServerStartupOptions m_options;
     bool m_useEnhancedSemanticTokens = false;
+    bool m_useSlangSemanticTokens = false;
 
     LanguageServerCore(LanguageServerStartupOptions options)
         : m_options(options)
